import { Home, Login } from '@mui/icons-material';

export const HOME = {
  path: '/',
  title: 'Home',
  icon: <Home />,
};

export const LOGIN = {
  path: '/login',
  title: 'Login',
  icon: <Login />,
};
