import { Page } from '../../components';
import { CATALOG } from '../../routes/routes';

const Catalog = () => {
  return (
    <Page title={CATALOG.title}>
      <h1>Catalog page. Coming soon...</h1>
    </Page>
  );
};

export default Catalog;
