import { Page } from '../../components';
import { REGISTRATION } from '../../routes/routes';

const Registration = () => {
  return <Page title={REGISTRATION.title}>Registration page</Page>;
};

export default Registration;
